const str = 'We are the best!'
console.log(str.endsWith('best!', 5))

const text = 'ES6 is short for ECMAScript'
const index = 4
console.log('the character at ' + index + ' is ' + text.charAt(index))

console.log(text.slice(4, 17))